# Secrets & credentials
.env
*.pem
*.key
secrets.json

# Dependencies
node_modules/
vendor/
.venv/
venv/
__pycache__/
*.pyc

# Build output
dist/
build/
*.o
*.exe

# Marker file that disables Start.bat's auto-update on a developer machine.
.no-auto-update

# OS noise
.DS_Store
Thumbs.db

# Local application data -- never commit a live database. The app reads DB_PATH
# (backend/database.py) so each install keeps its own copy outside the repo;
# committing one would overwrite a user's real sales/inventory on every update.
*.db
*.db-journal

# Logs & temp files
CLAUDE.md
*.csv
*.log
*.tmp
.cache/